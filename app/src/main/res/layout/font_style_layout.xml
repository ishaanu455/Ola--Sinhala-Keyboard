<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="?attr/fox_background">

    <!-- Same reasoning as clipboard_layout.xml: back arrow / title live in the
         keyboard's top_bar (btn_fonts), not a second header row here. -->

    <TextView
        android:id="@+id/font_style_note"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="14dp"
        android:paddingVertical="8dp"
        android:text="@string/font_style_english_only_note"
        android:textSize="12sp"
        android:textColor="?attr/foreground_2" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/font_style_list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:paddingHorizontal="2dp"
        android:paddingBottom="6dp" />

</LinearLayout>
